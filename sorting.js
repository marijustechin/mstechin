'use strict';

function dateInSecs() {
  return Math.round(Date.now() / 1000);
}

function sortArraySelection(array) {
  return sortedArray;
}

let arrayOfNumbers = [5, 9, 4, 8, 3, 12, 56, 98, 4, 2, 9, 4, 25];
